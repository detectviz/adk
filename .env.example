
# =============================
# SRE Assistant — .env.example
# 以 dotenv/Cloud Build substitutions 等方式注入環境變數
# =============================

# ---- 服務命名（優先影響 OTel service.name） ----
SERVICE_NAME=sre-assistant

# ---- Google OpenTelemetry 直投端點 ----
# 可由 Cloud Build substitutions 覆蓋 _OTEL_ENDPOINT
OTEL_EXPORTER_OTLP_ENDPOINT=https://otel.googleapis.com:4317
# 啟用 Google OTLP 身分驗證（ADC / GCP 環境）
GOOGLE_OTLP_AUTH=true

# ---- GCP 專案與區域（若 Cloud Run 已注入可忽略） ----
GOOGLE_CLOUD_PROJECT=your-project-id
CLOUD_RUN_REGION=us-central1

# ---- ADK 統一設定覆寫（對應 adk.yaml 的鍵） ----
# 將高風險命名空間以逗號列出，例如：prod,production,prd
ADK_POLICY_HIGH_RISK_NAMESPACES=prod,production,prd
# 需 HITL 審批的工具清單
ADK_AGENT_TOOLS_REQUIRE_APPROVAL=K8sRolloutRestartLongRunningTool
# 主代理名稱（若需覆蓋 adk.yaml.agent.name）
ADK_AGENT_NAME=sre-assistant

# ---- 安全與開發選項 ----
# 僅在本地開發時啟用；在生產請關閉或移除此行
ALLOW_DEV_KEY=false

# ---- OTel 開關（本地除錯時可關閉） ----
OTEL_ENABLED=true
