
version: "3.8"
services:
  sre-assistant:
    build: .
    image: sre-assistant:latest
    environment:
      - X_API_KEY=devkey
      - DEBOUNCE_TTL=5
      - CACHE_TTL_DEFAULT=20
    ports:
      - "8000:8000"
    volumes:
      - ./data:/mnt/data
